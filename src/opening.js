'use strict';

const HashTable = require('./hash_table');

module.exports = class Opening {
    constructor() {
        this.openingTable = new HashTable(20);
    }

    addPgn(pgn) {
    }

    remove(index) {
    }
};